{{- if .Values.active }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: hubble
  namespace: {{ .Release.Namespace }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: hubble
{{- end }}
